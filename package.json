{
  "name": "experimental-server-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "npm run build && node dist/init.js",
    "cluster": "npm run build && node dist/cluster.js",
    "threads": "npm run build && node dist/threads.js",
    "single": "npm run build && node dist/single.js",
    "multi": "npm run build && node dist/multi.js",
    "express": "ts-node src/express.ts",
    "limited": "ts-node src/limited.ts",
    "build": "tsc"
  },
  "author": "samlior",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.9",
    "@types/yallist": "^4.0.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.8.0",
    "sequelize": "^6.25.5",
    "yallist": "^4.0.0"
  }
}
